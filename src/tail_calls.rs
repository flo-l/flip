use ::value::Value;

// This checks if recur does only occur at tail positions in list
// it returns true if everything is fine and false if a wrong recur is detected
pub fn check_tail_calls(list: &[Value]) -> bool {
    true
}
